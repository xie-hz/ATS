<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.easymeeting.mappers.UserContactApplyMapper">

    <!--实体映射-->
    <resultMap id="base_result_map" type="com.easymeeting.entity.po.UserContactApply">
        <!--自增ID-->
        <id column="apply_id" property="applyId"/>
        <!--申请人id-->
        <result column="apply_user_id" property="applyUserId"/>
        <!--接收人ID-->
        <result column="receive_user_id" property="receiveUserId"/>
        <!--最后申请时间-->
        <result column="last_apply_time" property="lastApplyTime"/>
        <!--状态0:待处理 1:已同意  2:已拒绝 3:已拉黑-->
        <result column="status" property="status"/>
    </resultMap>


    <!-- 通用查询结果列-->
    <sql id="base_column_list">
        u.apply_id,u.apply_user_id,u.receive_user_id,u.last_apply_time,u.status

    </sql>

    <sql id="base_condition_filed">
        <if test="query.applyId != null">
            and u.apply_id = #{query.applyId}
        </if>
        <if test="query.applyUserId != null and query.applyUserId!=''">
            and u.apply_user_id = #{query.applyUserId}
        </if>
        <if test="query.receiveUserId != null and query.receiveUserId!=''">
            and u.receive_user_id = #{query.receiveUserId}
        </if>
        <if test="query.lastApplyTime != null">
            and u.last_apply_time = #{query.lastApplyTime}
        </if>
        <if test="query.status != null">
            and u.status = #{query.status}
        </if>
    </sql>

    <!-- 通用查询条件列-->
    <sql id="query_condition">
        <where>
            <include refid="base_condition_filed"/>
            <if test="query.applyUserIdFuzzy!= null  and query.applyUserIdFuzzy!=''">
                and u.apply_user_id like concat('%', #{query.applyUserIdFuzzy}, '%')
            </if>
            <if test="query.receiveUserIdFuzzy!= null  and query.receiveUserIdFuzzy!=''">
                and u.receive_user_id like concat('%', #{query.receiveUserIdFuzzy}, '%')
            </if>
        </where>
    </sql>

    <!-- 查询集合-->
    <select id="selectList" resultMap="base_result_map">
        SELECT
        <include refid="base_column_list"/>
        <if test="query.queryUserInfo">
            ,ui.nick_name nickName
        </if>
        FROM user_contact_apply u
        <if test="query.queryUserInfo">
            left join user_info ui on ui.user_id = u.apply_user_id
        </if>
        <include refid="query_condition"/>
        <if test="query.orderBy!=null">
            order by ${query.orderBy}
        </if>
        <if test="query.simplePage!=null">
            limit #{query.simplePage.start},#{query.simplePage.end}
        </if>
    </select>

    <!-- 查询数量-->
    <select id="selectCount" resultType="java.lang.Integer">
        SELECT count(1) FROM user_contact_apply u
        <include refid="query_condition"/>
    </select>

    <!-- 插入 （匹配有值的字段）-->
    <insert id="insert" parameterType="com.easymeeting.entity.po.UserContactApply">
        <selectKey keyProperty="bean.applyId" resultType="Integer" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
        INSERT INTO user_contact_apply
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="bean.applyUserId != null">
                apply_user_id,
            </if>
            <if test="bean.receiveUserId != null">
                receive_user_id,
            </if>
            <if test="bean.lastApplyTime != null">
                last_apply_time,
            </if>
            <if test="bean.status != null">
                status,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="bean.applyUserId!=null">
                #{bean.applyUserId},
            </if>
            <if test="bean.receiveUserId!=null">
                #{bean.receiveUserId},
            </if>
            <if test="bean.lastApplyTime!=null">
                #{bean.lastApplyTime},
            </if>
            <if test="bean.status!=null">
                #{bean.status},
            </if>
        </trim>
    </insert>

    <!-- 插入或者更新 （匹配有值的字段）-->
    <insert id="insertOrUpdate" parameterType="com.easymeeting.entity.po.UserContactApply">
        INSERT INTO user_contact_apply
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="bean.applyId != null">
                apply_id,
            </if>
            <if test="bean.applyUserId != null">
                apply_user_id,
            </if>
            <if test="bean.receiveUserId != null">
                receive_user_id,
            </if>
            <if test="bean.lastApplyTime != null">
                last_apply_time,
            </if>
            <if test="bean.status != null">
                status,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="bean.applyId!=null">
                #{bean.applyId},
            </if>
            <if test="bean.applyUserId!=null">
                #{bean.applyUserId},
            </if>
            <if test="bean.receiveUserId!=null">
                #{bean.receiveUserId},
            </if>
            <if test="bean.lastApplyTime!=null">
                #{bean.lastApplyTime},
            </if>
            <if test="bean.status!=null">
                #{bean.status},
            </if>
        </trim>
        on DUPLICATE key update
        <trim prefix="" suffix="" suffixOverrides=",">
            <if test="bean.applyUserId!=null">
                apply_user_id = VALUES(apply_user_id),
            </if>
            <if test="bean.receiveUserId!=null">
                receive_user_id = VALUES(receive_user_id),
            </if>
            <if test="bean.lastApplyTime!=null">
                last_apply_time = VALUES(last_apply_time),
            </if>
            <if test="bean.status!=null">
                status = VALUES(status),
            </if>
        </trim>
    </insert>

    <!-- 添加 （批量插入）-->
    <insert id="insertBatch" parameterType="com.easymeeting.entity.po.UserContactApply" useGeneratedKeys="true" keyProperty="applyId">
        INSERT INTO user_contact_apply(
        apply_user_id,
        receive_user_id,
        last_apply_time,
        status
        )values
        <foreach collection="list" item="item" separator=",">
            (
            #{item.applyUserId},
            #{item.receiveUserId},
            #{item.lastApplyTime},
            #{item.status}
            )
        </foreach>
    </insert>

    <!-- 批量新增修改 （批量插入）-->
    <insert id="insertOrUpdateBatch" parameterType="com.easymeeting.entity.po.UserContactApply">
        INSERT INTO user_contact_apply(
        apply_user_id,
        receive_user_id,
        last_apply_time,
        status
        )values
        <foreach collection="list" item="item" separator=",">
            (
            #{item.applyUserId},
            #{item.receiveUserId},
            #{item.lastApplyTime},
            #{item.status}
            )
        </foreach>
        on DUPLICATE key update
        apply_user_id = VALUES(apply_user_id),
        receive_user_id = VALUES(receive_user_id),
        last_apply_time = VALUES(last_apply_time),
        status = VALUES(status)
    </insert>

    <!--多条件修改-->
    <update id="updateByParam" parameterType="com.easymeeting.entity.query.UserContactApplyQuery">
        UPDATE user_contact_apply u
        <set>
            <if test="bean.applyUserId != null">
                apply_user_id = #{bean.applyUserId},
            </if>
            <if test="bean.receiveUserId != null">
                receive_user_id = #{bean.receiveUserId},
            </if>
            <if test="bean.lastApplyTime != null">
                last_apply_time = #{bean.lastApplyTime},
            </if>
            <if test="bean.status != null">
                status = #{bean.status},
            </if>
        </set>
        <include refid="query_condition"/>
    </update>

    <!--多条件删除-->
    <delete id="deleteByParam">
        delete u from user_contact_apply u
        <include refid="query_condition"/>
    </delete>

    <!-- 根据ApplyId修改-->
    <update id="updateByApplyId" parameterType="com.easymeeting.entity.po.UserContactApply">
        UPDATE user_contact_apply
        <set>
            <if test="bean.applyUserId != null">
                apply_user_id = #{bean.applyUserId},
            </if>
            <if test="bean.receiveUserId != null">
                receive_user_id = #{bean.receiveUserId},
            </if>
            <if test="bean.lastApplyTime != null">
                last_apply_time = #{bean.lastApplyTime},
            </if>
            <if test="bean.status != null">
                status = #{bean.status},
            </if>
        </set>
        where apply_id=#{applyId}
    </update>

    <!-- 根据ApplyId删除-->
    <delete id="deleteByApplyId">
        delete from user_contact_apply where apply_id=#{applyId}
    </delete>

    <!-- 根据PrimaryKey获取对象-->
    <select id="selectByApplyId" resultMap="base_result_map">
        select
        <include refid="base_column_list"/>
        from user_contact_apply u where apply_id=#{applyId}
    </select>

    <!-- 根据ApplyUserIdAndReceiveUserId修改-->
    <update id="updateByApplyUserIdAndReceiveUserId" parameterType="com.easymeeting.entity.po.UserContactApply">
        UPDATE user_contact_apply
        <set>
            <if test="bean.lastApplyTime != null">
                last_apply_time = #{bean.lastApplyTime},
            </if>
            <if test="bean.status != null">
                status = #{bean.status},
            </if>
        </set>
        where apply_user_id=#{applyUserId} and receive_user_id=#{receiveUserId}
    </update>

    <!-- 根据ApplyUserIdAndReceiveUserId删除-->
    <delete id="deleteByApplyUserIdAndReceiveUserId">
        delete from user_contact_apply where apply_user_id=#{applyUserId} and receive_user_id=#{receiveUserId}
    </delete>

    <!-- 根据PrimaryKey获取对象-->
    <select id="selectByApplyUserIdAndReceiveUserId" resultMap="base_result_map">
        select
        <include refid="base_column_list"/>
        from user_contact_apply u where apply_user_id=#{applyUserId} and receive_user_id=#{receiveUserId}
    </select>

</mapper>